class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        prefix=[0]*(len(gain)+1)
        for i,v in enumerate(gain):
            prefix[i+1]=prefix[i]+v
        return max(prefix)